{
  "app": "qizilsoz",
  "port": 3000,
  "disks": [        {
    "name": "prisma",
    "mountTo": "prisma/migrations"
}],
  "build": {
    "location": "iran",
    "commands": [
      "npm install",
      "npx prisma generate --schema=./prisma/schema.prisma",
      "npm run build"
    ]
  }
}
